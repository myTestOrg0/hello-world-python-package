name: Publish Python Package
on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  publish:
    environment: release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
      - uses: actions/checkout@v2
